# Notas del programa
```
Nota: En caso de que no llegue a funcionar el programa con cualquier version diferente de maven 3.8.1, instalar esta version en su lugar.

-- Limpiar los residuos de maven para evitar errores del programa: mvn clean
-- Instalar las dependencias de maven: mvn compile
-- Instalar las dependencias de Node para ejecucion del resultado de las pruebas: node install

# Ejecución del Proyecto

-- Archivos de configuracion se encuentran dentro de la carpeta src
-- Ejecucion del archivo se inicializa con (mvn test) o bien ejecutando el play de Java
```